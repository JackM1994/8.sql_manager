extends ../layout 
block content
    p
        |Are you sure you want to delete 
        em= book.title
        |?
    form(action="/books/" + book.id + "/delete", method="post")
        a(href="/books/" + books.id) Cancel
    |
    input(type="submit", value ="Yes, I'm sure")

  