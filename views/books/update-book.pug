extends ../layout
block content

    h1 Update Book
    include error
    form(action="/books/" + book.id, method="post")
      include form
      p
        input(type ="submit" value="Update Book")
   
      p
         a.button(href="/books") Cancel
      p
         a.button(href="/books/delete") Delete
